# render.yaml
# Render deployment configuration file

services:
  - type: web
    name: livestock-guardian-biometric
    env: python
    plan: free
    pythonVersion: "3.11.0"
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn fastapi_server:app --host 0.0.0.0 --port $PORT
